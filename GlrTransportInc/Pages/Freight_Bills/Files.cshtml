@page
@using GlrTransportInc.Models
@inject SignInManager<UserModel> SignInManager
@model GlrTransportInc.Pages.Freight_Bills.Files

@{
    ViewData["Title"] = "Files";
}

<!DOCTYPE html>

<html>
<head>
    <title></title>
</head>
<body>
@if (SignInManager.IsSignedIn(User))
{
    <div class="table-responsive" style="display: block !important;">
        <table class="table">
            <thead>
            <tr>
                <th>Files</th>
                <th></th>
                <th>Remove</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>File 1:</td>
                @if (Model.Filename1 != null)
                {
                    <td>
            
                        <a href="~/Permits/@Model.Filename1">@Model.Filename1</a>
                    </td>
                    <td>
                        <form method="post" enctype="multipart/form-data" class="col">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <a asp-page="./DeletePermit" asp-route-id="@Model.BillId" asp-route-fileNum="1" class="optionButton">
                                <img src="~/Images/delete-icon.png" style="width:25px; height:25px;" alt="Remove File">
                            </a>
                        </form>
                    </td>
                }
                else
                {
                    <td>

                        <form method="post" enctype="multipart/form-data" class="row">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="container-fluid column col-md-12 form-group">
                                <label class="btn btn-default row">
                                    <input type="file" asp-for="Upload" accept=".pdf" id="onlypdf"/>
                                </label>

                                <div class="form-group">
                                    <input type="submit" value="Save" class="btn btn-primary"/>
                                </div>

                            </div>
                        </form>
                    </td>
                    <td></td>
                }
            </tr>
            <tr>
                <td>File 2:</td>
                @if (Model.Filename2 != null)
                {
                    <td>
                        <a href="~/Permits/@Model.Filename2">@Model.Filename2</a>
                    </td>
                    <td>
                        <form method="post" enctype="multipart/form-data" class="col">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <a asp-page="./DeletePermit" asp-route-id="@Model.BillId" asp-route-fileNum="2" class="optionButton">
                                <img src="~/Images/delete-icon.png" style="width:25px; height:25px;" alt="Remove File">
                            </a>
                        </form>
                    </td>
                }
                else
                {
                    <td>
                        <form method="post" enctype="multipart/form-data" class="row">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="container-fluid column col-md-12 form-group">
                                <label class="btn btn-default row">
                                    <input type="file" asp-for="Upload2" accept=".pdf" id="onlypdf"/>
                                </label>

                                <div class="form-group">
                                    <input type="submit" value="Save" class="btn btn-primary"/>
                                </div>

                            </div>
                        </form>
                    </td>
                    <td></td>
                }
            </tr>
            <tr>
                <td>File 3:</td>
                @if (Model.Filename3 != null)
                {
                    <td>
                        <a href="~/Permits/@Model.Filename3">@Model.Filename3</a>
                    </td>
                    <td>
                        <form method="post" enctype="multipart/form-data" class="col">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <a asp-page="./DeletePermit" asp-route-id="@Model.BillId" asp-route-fileNum="3" class="optionButton">
                                <img src="~/Images/delete-icon.png" style="width:25px; height:25px;" alt="Remove File">
                            </a>
                        </form>
                    </td>
                }
                else
                {
                    <td>
                        <form method="post" enctype="multipart/form-data" class="row">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="container-fluid column col-md-12 form-group">
                                <label class="btn btn-default row">
                                    <input type="file" asp-for="Upload3" accept=".pdf" id="onlypdf"/>
                                </label>

                                <div class="form-group">
                                    <input type="submit" value="Save" class="btn btn-primary"/>
                                </div>

                            </div>
                        </form>
                    </td>
                    <td></td>
                }
            </tr>
            </tbody>
        </table>
    </div>
}
else
{
    <h4>Page is not available.</h4>
}
</body>
</html>